package com.knits.product.repository;

import com.knits.product.entity.ASNLinkDelivery;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ASNLinkDeliveryRepository extends JpaRepository<ASNLinkDelivery, Long> { }
